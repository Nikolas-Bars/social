
import React from 'react';
import {ActionTypes, SideBarFriendsType} from "./state";

const sidebarReducer = (state: SideBarFriendsType, action: ActionTypes) => {
        return state
    }


export default sidebarReducer